# MCP Surface (Current Status)

This project currently focuses on the `/crypto` web experience using:
- CopilotKit chat runtime via BFF
- LangGraph backend tools
- Controlled canvas rendering

A standalone MCP App flow is not part of the primary demo path right now.
If you enable/extend MCP later, keep it separate from the core crypto canvas flow.
